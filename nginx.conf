server {
    listen 80 default_server;
    server_name fast.infopluto.com;

    # Global HTTP to HTTPS Redirect
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl default_server;
    server_name fast.infopluto.com;

    # Points directly to where the Dockerfile copies your GitHub pem files
    ssl_certificate /data/fullchain.pem;
    ssl_certificate_key /data/privkey.pem;

    # Streaming optimizations for HLS over TLS
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    access_log /data/nginx_access.log combined;
    error_log /data/nginx_error.log warn;

    # HLS Streaming - Final Optimized for External HTTPS (SSAI)
    location /channel/ {
        proxy_pass http://127.0.0.1:5000;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;     # Force HTTPS context to Python backend
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
        proxy_set_header X-Forwarded-Ssl on;

        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }

    # TV Manager Control Panel
    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
